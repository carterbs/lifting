version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: docker/Dockerfile.prod
    ports:
      - "3001:3001"
    volumes:
      - lifting-data:/app/data
    environment:
      - NODE_ENV=production
    restart: unless-stopped

volumes:
  lifting-data:
